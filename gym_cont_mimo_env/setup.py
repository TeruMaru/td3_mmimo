from setuptools import setup

setup(name='gym_cont_mimo_env',
      version='0.0.1', # Add env_v1 for max sum training
      # And any other dependencies foo needs
      install_requires=['gym', 'numpy', 'scipy'])
